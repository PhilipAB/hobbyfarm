apiVersion: apps/v1
kind: Deployment
metadata:
  name: perf-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      component: perf-service
  template:
    metadata:
      labels:
        component: perf-service
    spec:
      containers:
        - name: perf-service
          image: {{ $.Values.perf.image }}
          command: ["performanceservice"]
          args:
          - "-v=9"
          - "-logtostderr"
          env:
            - name: LOG_LEVEL
              value: {{ $.Values.perf.logLevel | quote }}
            - name: GRPC_PORT
              value: {{ .Values.perf.grpcPort | quote }}
          ports:
          - containerPort: {{ .Values.perf.grpcPort }}
          volumeMounts:
          - name: auth-secret
            mountPath: "/etc/ssl/certs/ca.crt"
            subPath: ca.crt
            readOnly: true
          - name: auth-secret
            mountPath: "/etc/ssl/certs/tls.key"
            subPath: tls.key
            readOnly: true
          - name: auth-secret
            mountPath: "/etc/ssl/certs/tls.crt"
            subPath: tls.crt
            readOnly: true
      volumes:
      - name: auth-secret
        secret:
          secretName: hobbyfarm-auth-secret
          optional: false