{{ if $.Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-service
  namespace: {{ .Release.Namespace }}
  annotations:
  {{- range $key, $value := .Values.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
  {{- end }}
spec:
  rules:
  - host: {{ $.Values.ingress.hostnames.auth }}
    http:
      paths:
      - pathType: Prefix
        path: "/auth"
        backend:
          service:
            name: auth-service
            port: 
              number: 80
      - pathType: Prefix
        path: "/a/user"
        backend:
          service:
            name: user-service
            port: 
              number: 80
{{ if $.Values.ingress.tls.enabled }}
  tls:
  - hosts:
    - {{ $.Values.ingress.hostnames.auth }}
{{- if hasKey .Values.ingress.tls.secrets "auth" }}
    secretName: {{ $.Values.ingress.tls.secrets.auth }}
{{- end }}
{{ end }}
{{ end }}