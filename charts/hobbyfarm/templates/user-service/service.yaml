apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    component: user-service
  ports:
  - name: http
    port: 80
  - name: grpc
    protocol: TCP
    port: 8080
    targetPort: 8080
